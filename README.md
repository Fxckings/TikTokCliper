# 🎬 AutoTikTok - Генератор клипов для TikTok

Автоматически создавайте захватывающие вертикальные клипы для TikTok из длинных видео (сериалов, фильмов, подкастов) с использованием ИИ анализа и красивых анимированных субтитров.

## ✨ Основные возможности

- 🎯 **Умный анализ видео** - автоматически находит самые интересные моменты
- 🎬 **Детекция заставок** - пропускает вступительные заставки и рекламу
- 💡 **Контекстный поиск** - добавляет несколько секунд контекста перед интересным моментом
- 💬 **Пословные субтитры** - субтитры появляются синхронно с речью (по 2-3 слова)
- ✍️ **Умный перенос слов** - слова не разрываются на части (нет "прив" и "ет")
- 🎨 **Анимированные субтитры** - красивые эффекты появления (slide_up, fade_in, zoom_in)
- 📏 **Оптимальная длина** - клипы 15-90 секунд (идеально для сериалов)
- 🎯 **Автопоиск шрифтов** - автоматически находит системные шрифты
- ⚡ **Кэширование** - быстрая повторная обработка
- 🗣️ **Мультиязычность** - поддержка русского, английского и других языков
- 📱 **Вертикальный формат 9:16** - идеально для TikTok/Reels/Shorts
- 🎨 **Размытый фон** - для видео с неподходящим соотношением сторон
- 🔊 **Анализ звука и движения** - поиск динамичных сцен с речью

## 📋 Требования

- Python 3.8 <= Python 3.11
- FFmpeg (должен быть установлен отдельно)
- 4GB+ RAM (рекомендуется)
- Опционально: NVIDIA GPU с CUDA для ускорения

## 🚀 Установка

### 1. Установите FFmpeg

**macOS (через Homebrew):**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
- Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html)
- Добавьте в PATH

### 2. Клонируйте репозиторий

```bash
git clone https://github.com/yourusername/autotiktok.git
cd autotiktok
```

### 3. Создайте виртуальное окружение

```bash
python3 -m venv venv

source venv/bin/activate

```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

**Примечание:** При первом запуске будет загружена модель Whisper (~150MB для base). Это происходит автоматически.

## 🎯 Использование

**ВАЖНО:** Перед запуском активируйте виртуальное окружение!

```bash
source venv/bin/activate
```

### Базовое использование

Сгенерировать 5 клипов из видео:

```bash
python main.py video.mp4
```

### Расширенные опции

```bash
python main.py video.mp4 --clips 10

python main.py video.mp4 --language ru

python main.py video.mp4 --model medium

python main.py video.mp4 --output my_clips/

python main.py video.mp4 --min-duration 15 --max-duration 45

python main.py video.mp4 --no-blur

python main.py video.mp4 --verbose
```

### Справка по командам

```
usage: main.py [-h] [-n CLIPS] [-o OUTPUT] [-l LANGUAGE]
               [-m {tiny,base,small,medium,large}]
               [--min-duration MIN_DURATION] [--max-duration MAX_DURATION]
               [--no-blur] [--verbose] video

Аргументы:
  video                 Путь к входному видео файлу

Опции:
  -h, --help           Показать справку
  -n, --clips CLIPS    Количество клипов (по умолчанию: 5)
  -o, --output OUTPUT  Директория для сохранения (по умолчанию: clips)
  -l, --language LANG  Код языка (en, ru и т.д.) или авто-определение
  -m, --model MODEL    Размер модели Whisper (по умолчанию: base)
  --min-duration SEC   Минимальная длина клипа в секундах (по умолчанию: 15)
  --max-duration SEC   Максимальная длина клипа в секундах (по умолчанию: 90)
  --no-blur           Отключить размытый фон
  --verbose           Подробное логирование
```

## 🎨 Как это работает

1. **Детекция заставок** - Находит и пропускает вступительные заставки/рекламу
2. **Детекция сцен** - Использует PySceneDetect для поиска границ сцен
3. **Анализ аудио** - Анализирует энергию звука и находит речь (librosa, pydub)
4. **Анализ движения** - Вычисляет оптический поток между кадрами (OpenCV)
5. **Поиск пиков** - Находит самые интересные моменты внутри сцен
6. **Добавление контекста** - Добавляет 2-3 секунды ДО интересного момента
7. **Оценка сегментов** - Ранжирует на основе:
   - Интенсивность движения (30%)
   - Энергия звука (30%)
   - Плотность речи (20%)
   - Смена сцен (20%)
8. **Распознавание речи** - Транскрибирует аудио с помощью OpenAI Whisper
9. **Пословные субтитры** - Создает субтитры, синхронизированные с каждым словом
10. **Генерация клипов** - Создает вертикальные клипы с:
    - Формат 9:16 (вертикальный)
    - Пословные анимированные субтитры
    - Профессиональное кодирование

## 📊 Результат

Клипы сохраняются в директорию `clips/` (или указанную) с именами типа:

```
video_clip_1_00-15.mp4
video_clip_2_01-23.mp4
video_clip_3_04-56.mp4
```

Логи сохраняются в директорию `logs/` для отладки.

## ⚙️ Конфигурация

Продвинутые пользователи могут настроить параметры в `config.py`:

### Настройки видео
- `TARGET_WIDTH`, `TARGET_HEIGHT` - Разрешение выхода (по умолчанию: 1080x1920)
- `OUTPUT_BITRATE` - Качество видео (по умолчанию: 8000k)
- `OUTPUT_FPS` - Частота кадров (по умолчанию: 30)
- `MIN_CLIP_DURATION` - Минимальная длина клипа (по умолчанию: 15 секунд)
- `MAX_CLIP_DURATION` - Максимальная длина клипа (по умолчанию: 90 секунд)
- `PREFERRED_CLIP_DURATION` - Предпочтительная длина (по умолчанию: 50 секунд)

### Настройки анализа
- `SCENE_DETECTION_THRESHOLD` - Чувствительность детекции сцен (по умолчанию: 27.0)
- `MOTION_THRESHOLD` - Минимальный порог движения
- `AUDIO_ENERGY_PERCENTILE` - Процентиль энергии звука
- `CONTEXT_BEFORE_PEAK` - Секунд контекста перед пиком (по умолчанию: 3.0)

### Настройки субтитров
- `SUBTITLE_FONT_SIZE` - Размер шрифта (по умолчанию: 70)
- `SUBTITLE_FONT_COLOR` - Цвет субтитров (по умолчанию: yellow)
- `SUBTITLE_ANIMATION_ENABLED` - Включить анимацию (по умолчанию: True)
- `SUBTITLE_ANIMATION_TYPE` - Тип анимации: "slide_up", "fade_in", "zoom_in"
- `SUBTITLE_BOTTOM_MARGIN` - Отступ снизу (по умолчанию: 150px)
- `SUBTITLE_WORDS_PER_GROUP` - Слов в группе (по умолчанию: 3)
- `SUBTITLE_STROKE_COLOR` - Цвет обводки (по умолчанию: black)
- `SUBTITLE_STROKE_WIDTH` - Ширина обводки (по умолчанию: 4)

### Веса оценки
```python
WEIGHT_MOTION = 0.3
WEIGHT_AUDIO_ENERGY = 0.3
WEIGHT_SPEECH_DENSITY = 0.2
WEIGHT_SCENE_CHANGES = 0.2
```

## 🐛 Решение проблем

### FFmpeg не найден
```
Error: FFmpeg not found
```
**Решение:** Установите FFmpeg и добавьте в PATH.

### Нехватка памяти
```
Error: Out of memory
```
**Решение:**
- Используйте меньшую модель Whisper (`--model tiny` или `--model base`)
- Обрабатывайте более короткие видео
- Закройте другие приложения

### Медленная обработка
**Решения:**
- Используйте меньшую модель Whisper
- Включите GPU (требуется CUDA)
- Уменьшите количество клипов
- Понизьте разрешение в `config.py`

### Клипы не создаются
```
Error: No interesting segments found
```
**Решения:**
- Понизьте `MIN_SEGMENT_SCORE` в `config.py`
- Настройте `SCENE_DETECTION_THRESHOLD`
- Проверьте наличие звука в видео

## 📦 Структура проекта

```
autotiktok/
├── main.py
├── analyzer.py
├── clip_generator.py
├── subtitle_generator.py
├── config.py
├── utils.py
├── requirements.txt
├── README.md
├── .gitignore
├── LICENSE
├── clips/
├── logs/
└── temp/
```

## 🎨 Анимация субтитров

Доступны 3 типа анимации (настраивается в `config.py`):

1. **slide_up** (по умолчанию) - субтитры плавно поднимаются снизу
2. **fade_in** - плавное появление
3. **zoom_in** - увеличение при появлении

```python
SUBTITLE_ANIMATION_TYPE = "slide_up"
SUBTITLE_ANIMATION_ENABLED = True
```

## ⚡ Оптимизация и кэширование

### Автоматическое кэширование
Программа кэширует в `temp/cache/`:
- Обнаруженные сцены
- Загруженное аудио
- Сегменты речи
- Конец заставки

При повторном запуске анализ мгновенный!

### Оптимизации
- Уменьшенное разрешение для анализа движения (160x90)
- Семплирование кадров (5 fps)
- Эффективная работа с памятью
- Автоматический выбор FP16/FP32

## 📝 Лицензия

MIT License - см. файл LICENSE

## 🤝 Участие в разработке

Приветствуются любые предложения! Создавайте Pull Request.

1. Сделайте Fork репозитория
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 🙏 Благодарности

- [OpenAI Whisper](https://github.com/openai/whisper) - Распознавание речи
- [MoviePy](https://zulko.github.io/moviepy/) - Редактирование видео
- [PySceneDetect](https://github.com/Breakthrough/PySceneDetect) - Детекция сцен
- [librosa](https://librosa.org/) - Анализ аудио

## 📧 Поддержка

Для вопросов и предложений:
- Откройте issue на GitHub
- Проверьте существующие issues

## 🎓 Примеры использования

### Пример 1: Подкаст в TikTok
```bash
python main.py podcast.mp4 --clips 8 --language en
```

### Пример 2: Русский контент
```bash
python main.py russian_series.mp4 --language ru --model medium
```

### Пример 3: Высокое качество
```bash
python main.py video.mp4 --model large --clips 3
```

### Пример 4: Быстрая обработка
```bash
python main.py video.mp4 --model tiny --clips 5
```

## 🆕 Что нового

### Улучшения v3.2

- ✅ **Исправлен Segmentation Fault** - убрана многопоточность из-за Whisper thread-safety
- ✅ **Исправлена ошибка soundfile** - альтернативный метод загрузки аудио через FFmpeg
- ✅ **Полное подавление h264 ошибок** - максимальное игнорирование ошибок декодирования
- ✅ **Стабильная работа** - последовательная обработка без крашей
- ✅ **Прогресс-бар** - визуальный прогресс при экспорте каждого клипа

### Улучшения v3.1

- ✅ **Исправлены зависания** - улучшена стабильность экспорта видео
- ✅ **Прогресс-бар** - визуальный прогресс при экспорте
- ✅ **Обработка ошибок h264** - игнорирование несущественных ошибок декодирования
- ✅ **Быстрый пресет** - использует ultrafast для ускорения

### Улучшения v3.0

- ✅ **Детекция заставок** - автоматически пропускает вступительные заставки и рекламу
- ✅ **Контекстный поиск** - добавляет 2-3 сек перед интересным моментом
- ✅ **Пословные субтитры** - слова появляются синхронно с речью
- ✅ **Умный перенос** - слова не разрываются на части
- ✅ **Оптимизированный код** - удалены все комментарии для чистоты кода

### Технические улучшения

- 🏗️ Детекция черных экранов и статичных изображений
- 💾 Кэширование результатов анализа заставок
- 🎯 Поиск пиков интересности внутри сцен
- 📊 Пословная временная разметка от Whisper
- 🚀 Улучшенная производительность
- 🛡️ Устойчивость к ошибкам декодирования
- 🔧 Альтернативная загрузка аудио через FFmpeg
- 📊 Последовательная обработка для стабильности

## 🔮 Планы на будущее

- [ ] Автоматическая фоновая музыка
- [ ] Генерация хэштегов с помощью AI
- [ ] Прямая загрузка в TikTok API
- [ ] Batch обработка нескольких видео
- [ ] Web интерфейс
- [ ] Превью в реальном времени

---

Сделано с ❤️ для создателей контента от useanasha
# TikTokCliper
